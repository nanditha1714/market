{
  "name": "market-research-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd react-app && npm install && cd ../server && npm install",
    "build": "cd react-app && npm run build",
    "start": "cd server && node index.js"
  }
}
